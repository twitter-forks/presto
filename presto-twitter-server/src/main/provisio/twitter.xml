<runtime>
    <!-- Target -->
    <archive name="${project.artifactId}-${project.version}.tar.gz" />

    <artifactSet to="plugin/twitter-functions">
        <artifact id="${project.groupId}:presto-twitter-functions:zip:${project.version}">
            <unpack />
        </artifact>
    </artifactSet>

    <artifactSet to="plugin/twitter-event-listener">
        <artifact id="${project.groupId}:presto-twitter-event-listener:zip:${project.version}">
            <unpack />
        </artifact>
    </artifactSet>
</runtime>
